export enum TypeEnum {
    STRING      = 'string', // Subtypes required
    NUMBER      = 'number',
    FLOAT       = 'float',
    BOOLEAN     = 'boolean',
    DATETIME    = 'datetime',
    DATE        = 'date',
    TIME        = 'time',
    ENTITY      = 'many to one',
    ENTITIES    = 'one to many'
}